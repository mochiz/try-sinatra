<article>
  <header>
    <h1>メモアプリ</h1>
  </header>
  <section>
    <ul>
      <% @memos.each do |memo| %>
        <li>
          <a href='<%= "/memos/#{memo.id}" %>'> <%= h memo.title %></a>
        </li>
      <% end %>
    </ul>
    <div class="buttons">
      <div class="button">
        <a href="/memos/new">追加</a>
      </div>
    </div>
  </section>
</article>
